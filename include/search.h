#ifndef SEARCH_
#define SEARCH_

#include <iostream>
#include <chrono>
#include <iterator>
#include <cmath>
#include <algorithm>

int *lin_search(int *first, int *last, int target);
int *bsearch(int *first, int *last, int target);

#endif
