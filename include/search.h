#ifndef SEARCH_
#define SEARCH_

#include <iostream>
#include <chrono>
#include <algorithm>

int *lsearch(int *first, int *last, short target);
int *bsearch(int *first, int *last, short target);

#endif
